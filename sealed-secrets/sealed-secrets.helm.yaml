apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: sealed-secrets
  namespace: flux-system
spec:
  chart:
    spec:
      chart: sealed-secrets
      sourceRef:
        kind: HelmRepository
        name: stable
  values:
    ingress:
      enabled: true
      hosts: ["secrets.192.168.178.200.nip.io"]
  interval: 5m0s
  releaseName: sealed-secrets
  targetNamespace: kube-system